@* File: _Groupsidebar.cshtml *@
@using Cloud9_2.Helpers
@inject Microsoft.AspNetCore.Mvc.Rendering.IHtmlHelper<dynamic> Html

@* --- MODIFICATION --- *@
@* Add class="left-sidebar" and remove inline style="width: 200px;" *@
<div class="left-sidebar d-flex flex-column flex-shrink-0"> @* Keep border-end or remove if handled by CSS *@
@* --- END MODIFICATION --- *@

    <div class="pt-3">
        @if (User.IsInRole("SuperAdmin") || User.IsInRole("Admin") || User.IsInRole("GroupRemoteUser"))
        {
<ul class="nav flex-column">
  <li class="nav-item">

    <a class="nav-link px-3 py-2 @(Html.IsActive("TasksIntezkedes/TasksIntezkedes", "GroupRemote") ? "active" : "")"
       asp-page="/GroupRemote/TasksIntezkedes/TasksIntezkedes">
      <i class="bi bi-clipboard-check me-2"></i>Intézkedések
    </a>

    <a class="nav-link px-3 py-2 @(Html.IsActive("TaskBejelentes/TaskBejelentes", "GroupRemote") ? "active" : "")"
       asp-page="/GroupRemote/TaskBejelentes/TaskBejelentes">
      <i class="bi bi-megaphone me-2"></i>Bejelentések
    </a>

    <a class="nav-link px-3 py-2 @(Html.IsActive("Tasks/Index", "GroupRemote") ? "active" : "")"
       asp-page="/GroupRemote/Tasks/Index">
      <i class="bi bi-list-task me-2"></i>Feladatok
    </a>

    <a class="nav-link px-3 py-2 @(Html.IsActive("Resources/Index", "GroupRemote") ? "active" : "")"
       asp-page="/GroupRemote/Resources/Index">
      <i class="bi bi-hdd-stack me-2"></i>Eszközök
    </a>

    <a class="nav-link px-3 py-2 @(Html.IsActive("Projects/Index", "GroupRemote") ? "active" : "")"
       asp-page="/GroupRemote/Projects/Index">
      <i class="bi bi-kanban me-2"></i>Projektek
    </a>

    <a class="nav-link px-3 py-2 @(Html.IsActive("Meetings/Index", "GroupRemote") ? "active" : "")"
       asp-page="/GroupRemote/Meetings/Index">
      <i class="bi bi-calendar-event me-2"></i>Megbeszélések
    </a>

    <a class="nav-link px-3 py-2 @(Html.IsActive("TaskType/Index", "GroupRemote") ? "active" : "")"
       asp-page="/GroupRemote/TaskType/Index">
      <i class="bi bi-tags me-2"></i>Feladat típusok
    </a>

    <a class="nav-link px-3 py-2 @(Html.IsActive("TaskStatuses/Index", "GroupRemote") ? "active" : "")"
       asp-page="/GroupRemote/TaskStatuses/Index">
      <i class="bi bi-flag me-2"></i>Feladat státuszok
    </a>

    <a class="nav-link px-3 py-2 @(Html.IsActive("TaskPriorities/Index", "GroupRemote") ? "active" : "")"
       asp-page="/GroupRemote/TaskPriorities/Index">
      <i class="bi bi-exclamation-circle me-2"></i>Feladat prioritások
    </a>

    <a class="nav-link px-3 py-2 @(Html.IsActive("ResourceTypes/Index", "GroupRemote") ? "active" : "")"
       asp-page="/GroupRemote/ResourceTypes/Index">
      <i class="bi bi-box-seam me-2"></i>Eszköz típusok
    </a>

    <a class="nav-link px-3 py-2 @(Html.IsActive("ResourceStatuses/Index", "GroupRemote") ? "active" : "")"
       asp-page="/GroupRemote/ResourceStatuses/Index">
      <i class="bi bi-toggle-on me-2"></i>Eszköz státuszok
    </a>

  </li>
</ul>

        }
    </div>
</div>