@page
@model EnableAuthenticatorModel
@using QRCoder
@using System.Drawing
@using System.Drawing.Imaging
@{
    Layout = null; // Ensure it loads properly inside a modal
    ViewData["Title"] = "Configure authenticator app";
    ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

<partial name="_StatusMessage" for="StatusMessage" />
<h3>@ViewData["Title"]</h3>
<div>
    <p>A hitelesítő alkalmazás használatához a következő lépéseken kell végigmennie:</p>
    <ol class="list">
        <li>
            <p>
                Töltsön le egy kétfaktoros hitelesítési alkalmazást, például a Microsoft Authenticator: 
                <a href="https://go.microsoft.com/fwlink/?Linkid=825072">Android</a> és
                <a href="https://go.microsoft.com/fwlink/?Linkid=825073">iOS</a> vagy
                Google Authenticator: 
                <a href="https://play.google.com/store/apps/details?id=com.google.android.apps.authenticator2&amp;hl=en">Android</a> és
                <a href="https://itunes.apple.com/us/app/google-authenticator/id388497605?mt=8">iOS</a>.
            </p>
        </li>
        <li>
            <p>Szkennelje be a QR-kódot vagy adja meg ezt a kulcsot <kbd>@Model.SharedKey</kbd> a kétfaktoros hitelesítési alkalmazásba. A szóközök nem számítanak.</p>
            <div class="alert alert-info">Tanulja meg, hogyan <a href="https://go.microsoft.com/fwlink/?Linkid=852423">engedélyezze a QR-kód generálását</a>.</div>

            @{
                // Generate the QR code using QRCoder
                QRCodeGenerator qrGenerator = new QRCodeGenerator();
                QRCodeData qrCodeData = qrGenerator.CreateQrCode(Model.AuthenticatorUri, QRCodeGenerator.ECCLevel.Q);
                BitmapByteQRCode qrCode = new BitmapByteQRCode(qrCodeData);

                // Adjust the size of the QR code (e.g., 10 pixels per module)
                byte[] qrCodeImage = qrCode.GetGraphic(10);

                // Convert the QR code to a base64 string for embedding in HTML
                string qrCodeImageBase64 = Convert.ToBase64String(qrCodeImage);
            }

            <!-- Display the QR code as an image -->
            <div id="qrCode">
                <img src="data:image/png;base64,@qrCodeImageBase64" alt="QR Code" style="width: 200px; height: 200px;" />
            </div>
            <div id="qrCodeData" data-url="@Model.AuthenticatorUri"></div>
        </li>
        <li>
            <p>
                Miután beolvasta a QR-kódot, vagy beírta a fenti kulcsot, a kétfaktoros hitelesítési alkalmazás a következőket fogja biztosítani Önnek
                egy egyedi kódot. Írja be a kódot az alábbi megerősítő mezőbe.
            </p>
            <div class="row">
                <div class="col-md-6">
                    <form id="send-code" method="post">
                        <div class="form-floating mb-3">
                            <input asp-for="Input.Code" class="form-control" autocomplete="off" placeholder="Please enter the code."/>
                            <label asp-for="Input.Code" class="control-label form-label">Ellenőrző kód</label>
                            <span asp-validation-for="Input.Code" class="text-danger"></span>
                        </div>
                        <button type="submit" class="w-100 btn btn-lg btn-primary">Ellenőrizze a  címet.</button>
                        <div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>
                    </form>
                </div>
            </div>
        </li>
    </ol>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}